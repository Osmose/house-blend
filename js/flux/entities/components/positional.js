define(function(require) {
    // Positional
    return {
        initialize: function(options) {
            this.x = options.x || 0;
            this.y = options.y || 0;
        },

        move: function(dx, dy) {

        }
    };
});
